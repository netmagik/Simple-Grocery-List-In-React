import React from 'react';

class Products extends React.Component {
    render() {
        return (
            
            <span>
                {/* {Object.keys(this.props.productData).map(key => ({key}))}
                {this.props.productData} */}
            </span>
            
        );
    }
}

export default Products;